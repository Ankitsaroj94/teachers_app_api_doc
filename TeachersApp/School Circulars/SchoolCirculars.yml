info:
  name: SchoolCirculars
  type: http
  seq: 1

http:
  method: GET
  url: "{{staffBase}}Staff/SchoolCirculars"
  body:
    type: json
    data: |-
      {
        "SiteId": 47,
        "SessionId": 768,
        "UserSysId": 14906,
        "isStarred": "N"
      }
  auth:
    type: bearer
    token: "{{staffToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
