info:
  name: Staff Leave Request Application
  type: http
  seq: 3

http:
  method: GET
  url: "{{staffBase}}Staff/StaffLeaveRequestApplication"
  body:
    type: json
    data: |-
      {
        "UserSysId": 555,
        "SiteId": 18,
        "AsOnDate": "Wednesday — 21 May ",
        "Type": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
