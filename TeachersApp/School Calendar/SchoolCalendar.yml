info:
  name: SchoolCalendar
  type: http
  seq: 1

http:
  method: GET
  url: "{{staffBase}}Staff/SchoolCalendar"
  body:
    type: json
    data: |-
      {
        "SiteId": 47,
        "SessionId": 768,
        "UserSysId": 14906,
        "FromDate": "01 JAN 2026",
        "ToDate": "30 APR 2026",
        "isStarred": "N"
      }
  auth:
    type: bearer
    token: "{{staffToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
