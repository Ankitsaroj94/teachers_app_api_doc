info:
  name: Student Personality Traits
  type: http
  seq: 3

http:
  method: GET
  url: "{{staffBase}}Staff/StudentPersonalityTraits"
  body:
    type: json
    data: |-
      {
        "StudentId": 175949,
        "SessionId": 640,
        "UserSysId": 555
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
